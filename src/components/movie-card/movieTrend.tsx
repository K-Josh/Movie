import { MovieDataType } from "../../assets/data"


interface MovieTrendCardProps {
    movie: MovieDataType;
}

const MovieCard = ({movie}: MovieTrendCardProps) => {
  return (
    <div>MovieCard</div>
  )
}

export default MovieCard